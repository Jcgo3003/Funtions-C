/* Utilizando funcion sqrt de math */
#include<stdio.h>
#include<math.h>

int main(void)
{
    /* declarando variables */
    int c = 4;

    /* Imprimiendo el resultado */
    printf("La raiz cuadrada de 4 es %.0f\n", sqrt( c ) );

    return 0;
}